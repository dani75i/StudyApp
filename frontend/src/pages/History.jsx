import React, { useEffect, useState } from 'react';import {CheckCircle2,XCircle} from 'lucide-react';import {api} from '../api';
export default function History(){const[data,setData]=useState(null);useEffect(()=>{api('/history').then(setData)},[]);if(!data)return <div className="loader-page"><div className="loader"/></div>;return <><header className="page-header"><div><span className="eyebrow">ACTIVITÉ</span><h1>Historique</h1><p>Les 50 dernières réponses enregistrées sur ton compte.</p></div></header><div className="history-table">{data.length?data.map(x=><div className="history-row" key={x.id}><div className={x.is_correct?'result-icon ok':'result-icon ko'}>{x.is_correct?<CheckCircle2/>:<XCircle/>}</div><div className="history-main"><strong>{x.exercise}</strong><span>{x.subject} • {x.chapter}</span></div><div className="history-answer">{x.answer}</div><time>{new Date(x.created_at).toLocaleString('fr-FR',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'})}</time></div>):<div className="empty">Tu n'as encore répondu à aucun exercice.</div>}</div></>}
